/**
 * Jest setup file for @elder-first/web
 *
 * NOTE: Do not move web/UI tests into packages/types.
 * If apps/web lacks a test harness, set one up here instead of
 * using packages/types as a dumping ground.
 *
 * packages/types is reserved for shared types and schemas only.
 */

// Add any global test setup here
// For example, if using @testing-library/jest-dom:
// import '@testing-library/jest-dom';

// For now, just basic setup for logic tests
export {};
